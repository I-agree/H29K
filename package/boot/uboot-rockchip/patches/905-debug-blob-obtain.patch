--- a/tools/binman/etype/blob.py
+++ b/tools/binman/etype/blob.py
@@ -45,4 +45,7 @@
     def ObtainContents(self, fake_size: int = 0) -> bool:
         self._filename = self.GetDefaultFilename()
+        from u_boot_pylib import tout
+        tout.info(f"[DEBUG blob ObtainContents] after GetDefaultFilename, self._filename={repr(self._filename)}, type={type(self._filename)}")
+        tout.info(f"[DEBUG blob ObtainContents] self.external={self.external}, self.optional={self.optional}")
         self._pathname = tools.get_input_filename(self._filename,
             self.external and (self.optional or self.section.GetAllowMissing()))
