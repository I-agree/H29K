--- a/tools/binman/etype/rockchip_tpl.py
+++ b/tools/binman/etype/rockchip_tpl.py
@@ -8,9 +8,17 @@
 class Entry_rockchip_tpl(Entry_blob_named_by_arg):
     def __init__(self, section, etype, node):
         super().__init__(section, etype, node, 'rockchip-tpl')
+        import sys
+        from u_boot_pylib import tout
+        tout.info(f"[DEBUG rockchip-tpl __init__ after super] self._filename = {repr(self._filename)}, type={type(self._filename)}")
         self.external = True
+        if self._filename is None:
+            tout.warning("[DEBUG rockchip-tpl] Fix self._filename None -> empty string")
+            self._filename = ""

     def ObtainContents(self, fake_size=0):
+        from u_boot_pylib import tout
+        tout.info(f"[DEBUG rockchip-tpl ObtainContents entry] self._filename = {repr(self._filename)}, type={type(self._filename)}")
         if self._filename is None:
             self._filename = ""
         return super().ObtainContents(fake_size)
