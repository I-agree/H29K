--- a/tools/binman/etype/rockchip_tpl.py
+++ b/tools/binman/etype/rockchip_tpl.py
@@ -18,3 +18,15 @@
     def __init__(self, section, etype, node):
         super().__init__(section, etype, node, 'rockchip-tpl')
         self.external = True
+        hard_path = "/workdir/openwrt/staging_dir/target-aarch64_generic_musl/image/rk3528_ddr_1056MHz_v1.11.bin"
+        self._fixed_path = hard_path
+
+    def GetDefaultFilename(self):
+        return self._fixed_path
+
+    def ObtainContents(self, fake_size=0):
+        fname = self.GetDefaultFilename()
+        pathname = tools.get_input_filename(fname, self.external and (self.optional or self.section.GetAllowMissing()))
+        if pathname is None:
+            self.Raise(f"Hard-coded DDR TPL file missing at: {fname}")
+        return super().ObtainContents(fake_size)
